#-*-coding:utf-8-*-          # 声明编码
#!d:/python/python38         # 声明路径
'''
下面开始编程
'''
#开始
x=input('first:');y=input('second:')      # 这是在定义变量

print(x+y,              # 这是在换行
      x+y+y,
      x+x+y,
      )
			
以上是最近写的