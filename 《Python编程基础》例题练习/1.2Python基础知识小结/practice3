>>> pi=3.14
>>> r=3
>>> C=2*pi*r
>>> S=pi*r**2
>>> print('半径为',r,'的圆形，其周长等于',C,'，面积等于',S,'。')
半径为 3 的圆形，其周长等于 18.84 ，面积等于 28.26 。

>>> S=5
>>> pi=3.14
>>> r=round((S/pi)**0.5,2)                                                        # round(number,num_digits)：按指定的位数对数值四舍五入，只保留两位小数
>>> C=round(2*pi*r,2)
>>> str_print='面积为'+str(S)+'的圆形，其半径为'+str(r)+'；周长等于'+str(C)+'。'      # str()：返回一个对象的string格式(用到了字符串的拼接)
>>> print(str_print)
面积为5的圆形，其半径为1.26；周长等于7.91。